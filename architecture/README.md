# Architecture

| File | Subject |
|---|---|
| `agent-loop-primer.html` | UML primer on `course/demo/`: the nine-step loop that reconciles invoices, spends one judgment per unit and proves each proposal |
| `workshop-architecture.html` | 4+1 view model [1] of `course/workshop/`: the tooling that compiles a use case into a validated design and, on named acceptance, scaffolds a stub loop |

Open either HTML file in a browser; each links to the other. Diagrams render with Mermaid 11.4.1 from cdn.jsdelivr.net, so a network connection is required.

[1] P. B. Kruchten, "The 4+1 view model of architecture," *IEEE Software*, vol. 12, no. 6, pp. 42–50, Nov. 1995.
