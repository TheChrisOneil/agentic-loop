# The design this loop was built from

**Use case:** Refund requests over 500 dollars, roughly 120 a week across three storefronts

| | |
|---|---|
| Unit of work | One refund request, with the order it refers to |
| Approver | M. Okafor, Refunds Team Lead |
| Fan-out | 10 |
| Exit criterion | Shut down if wrong refunds released exceeds 1 in any quarter |

## Assumptions this design rests on

- Order history and prior refunds are retrievable from the payments system by account
- The reason code is chosen by the agent raising the request, not by the customer
- Storefront refund policy is identical across all three brands

## The flow

```mermaid
%% Generated by render.sh from the design. Do not edit by hand —
%% edit the design and render again. This diagram cannot disagree with it.
sequenceDiagram
    autonumber
    participant C as Clock
    participant L as Loop
    participant X as Expert
    participant H as M. Okafor, Refunds Team Lead
    participant G as Ledger

    Note over L,G: every transition below is appended to the ledger
    C->>L: tick — Refund requests over 500 dollars, roughly 120 a week ac…

    L->>L: 1 fetch — Pull refund requests over 500 dollars from the payments sys…
    L->>L: 2 enrich — Attach order history, prior refunds and the stated reason c…
    L->>L: 3 select — Order by value and age, capped at the daily budget
    alt requested amount is more than 2000 USD, or the account has more than 3 …
        L->>H: REFUSE — Send to the fraud desk with the account history attached before any amount is released
        L-)G: refused
        Note over L,H: this unit stops here, with a named next action
    else the gate admits it
        L->>L: 4 gate-intake — Refuse high-value or repeat-refund accounts before anything…
    end
    L->>X: 5 judge — Decide whether the stated reason is consistent with the ord…
    X-->>L: a call, with the evidence they cite
    L->>L: 6 recheck — Recompute the refundable amount from the original order and…
    alt the recomputed refundable amount differs from the requested amount
        L->>H: REFUSE — Return both figures to the agent who raised the request and have a person decide which is right
        L-)G: refused
        Note over L,H: this unit stops here, with a named next action
    else the gate admits it
        L->>L: 7 gate-amount — Refuse when the recomputed amount disagrees with the request
    end
    L->>L: 8 prove — Write the order, the history, the recomputation and the dec…
    L->>H: 9 deliver — Write a release proposal assigned to the named approver
    L-)G: delivered
    Note over H: nothing is approved here. A named person approves it, or does not.
    H-->>L: approved, or returned with a reason
    alt proof checksum differs from the checksum recorded when it was written
        L->>H: REFUSE — Stop the release and have the refunds owner establish who edited the proof and when
        L-)G: refused
        Note over L,H: this unit stops here, with a named next action
    else the gate admits it
        L->>L: proceed
    end
```

## The KPIs it is governed by

| KPI | Kind | Baseline |
|---|---|---|
| cost per completed decision | cost | unmeasured |
| wrong refunds released | quality | 2 in the last quarter |
| days from request to release | throughput | 6 days |
| refusal rate | control | not applicable before this loop |

Regenerate this file by re-running the scaffolder. Edit `design/loop.design`, never this.
