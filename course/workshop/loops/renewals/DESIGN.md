# The design this loop was built from

**Use case:** Contract renewals arriving with changed terms, about 40 a quarter

| | |
|---|---|
| Unit of work | One renewal, with its changed clauses grouped by the obligation they affect |
| Approver | J. Lindqvist, Commercial Counsel |
| Fan-out | 4 |
| Exit criterion | Shut down if a material change is missed more than 1 time in any 12 months |

## Assumptions this design rests on

- The signed version of every contract is retrievable and is the correct comparison baseline
- Clause section identifiers are stable between the signed version and the renewal
- Counterparties send renewals as text, not as scanned images

## The flow

```mermaid
%% Generated by render.sh from the design. Do not edit by hand —
%% edit the design and render again. This diagram cannot disagree with it.
sequenceDiagram
    autonumber
    participant C as Clock
    participant L as Loop
    participant M as Model
    participant H as J. Lindqvist, Commercial Counsel
    participant G as Ledger

    Note over L,G: every transition below is appended to the ledger
    C->>L: tick — Contract renewals arriving with changed terms, about 40…

    L->>L: 1 fetch — Pull renewals due within 60 days and diff each against the …
    L->>L: 2 group — Group changed clauses by section identifier into candidate …
    L->>L: 3 select — Order by contract value and renewal date, capped at the bud…
    alt contract value is over 250000 USD, or the counterparty is on the restri…
        L->>H: REFUSE — Route to counsel before any clause is assessed, with the full diff attached
        L-)G: refused
        Note over L,H: this unit stops here, with a named next action
    else the gate admits it
        L->>L: 4 gate-scope — Refuse high-value or restricted counterparties before any c…
    end
    L->>M: 5 assess — Decide whether a changed clause is cosmetic, material, or a…
    M-->>L: a call, with the evidence it cites
    L->>L: 6 crosscheck — Confirm every changed clause appears in exactly one unit an…
    alt a changed clause appears in no unit, or in more than one
        L->>H: REFUSE — Return the clause list to the contracts owner and have a person reconcile it before review
        L-)G: refused
        Note over L,H: this unit stops here, with a named next action
    else the gate admits it
        L->>L: 7 gate-coverage — Refuse when a clause belongs to no unit or to more than one
    end
    L->>L: 8 prove — Write the diff, the grouping, the coverage result and the a…
    L->>H: 9 deliver — Write a review pack assigned to the named approver
    L-)G: delivered
    Note over H: nothing is approved here. A named person approves it, or does not.
    H-->>L: approved, or returned with a reason
    alt proof checksum differs from the checksum recorded when it was written
        L->>H: REFUSE — Stop the review pack and have the contracts owner establish who edited the proof and when
        L-)G: refused
        Note over L,H: this unit stops here, with a named next action
    else the gate admits it
        L->>L: proceed
    end
```

## The KPIs it is governed by

| KPI | Kind | Baseline |
|---|---|---|
| cost per completed decision | cost | unmeasured |
| material changes missed | quality | 1 in the last 12 months |
| days from receipt to counsel review | throughput | 11 days |
| refusals per 100 renewals | control | not applicable before this loop |

Regenerate this file by re-running the scaffolder. Edit `design/loop.design`, never this.
