# The design this loop was built from

**Use case:** Supplier invoices that do not match their purchase order, roughly 400 a month

| | |
|---|---|
| Unit of work | One (supplier, purchase order) pair |
| Approver | A. Rivera, Accounts Payable |
| Fan-out | 7 |
| Exit criterion | Shut down if review minutes per unit exceeds 4 for two consecutive weeks |

## Assumptions this design rests on

- Invoice and purchase order lines are retrievable from the ERP as structured records
- A supplier reusing a purchase order number across periods is out of scope for now
- Tolerance limits are set by finance policy and reviewed quarterly
- The supervised supplier list is maintained by the contract owner, not by this loop

## The flow

```mermaid
%% Generated by render.sh from the design. Do not edit by hand —
%% edit the design and render again. This diagram cannot disagree with it.
sequenceDiagram
    autonumber
    participant C as Clock
    participant L as Loop
    participant M as Model
    participant H as A. Rivera, Accounts Payable
    participant G as Ledger

    Note over L,G: every transition below is appended to the ledger
    C->>L: tick — Supplier invoices that do not match their purchase orde…

    L->>L: 1 decompose — Group invoice lines and purchase order lines into units by …
    L->>L: 2 match — Reconcile each unit by arithmetic and screen memo text for …
    L->>L: 3 select — Choose what is worked tonight, worst first, under a budget
    alt exposure over 2000 USD, or supplier on the supervised list, or no purch…
        L->>H: REFUSE — Route to a second approver, or raise a purchase order for this spend before any amount is appro…
        L-)G: refused
        Note over L,H: this unit stops here, with a named next action
    else the gate admits it
        L->>L: 4 gate-pre — Refuse no-PO, injection-suspect, supervised supplier, or ov…
    end
    L->>M: 5 judge — Decide whether a variance is a dispute, discount, duplicate…
    M-->>L: a call, with the evidence it cites
    L->>L: 6 verify — Recompute the claimed adjustment from the raw files and com…
    alt recomputed adjustment differs from the claimed adjustment by more than …
        L->>H: REFUSE — Return both figures to the reviewer and have a person decide which is right
        L-)G: refused
        Note over L,H: this unit stops here, with a named next action
    else the gate admits it
        L->>L: 7 gate-post — Refuse an unusable call, missing evidence, or an unverified…
    end
    L->>L: 8 prove — Write the arithmetic, the commands and the judgment, then c…
    alt proof checksum differs from the checksum recorded when it was written
        L->>H: REFUSE — Stop delivery and have the loop owner establish who edited the proof and when
        L-)G: refused
        Note over L,H: this unit stops here, with a named next action
    else the gate admits it
        L->>L: proceed
    end
    L->>H: 9 deliver — Re-check the checksum and write a proposal for the named ap…
    L-)G: delivered
    Note over H: nothing is approved here. A named person approves it, or does not.
    H-->>L: approved, or returned with a reason
```

## The KPIs it is governed by

| KPI | Kind | Baseline |
|---|---|---|
| cost per completed decision | cost | unmeasured |
| escaped defect rate | quality | 0 known in the last quarter |
| review minutes per unit | throughput | 9 minutes manual |
| refusal rate | control | not applicable before this loop |

Regenerate this file by re-running the scaffolder. Edit `design/loop.design`, never this.
